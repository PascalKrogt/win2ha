# win2ha
Lightweight Python service that links Windows and Home Assistant via MQTT. Uses MQTT Discovery to auto‑create a switch entity and run predefined safe commands when toggled. Runs silently in the background via Task Scheduler or as a Windows service.
